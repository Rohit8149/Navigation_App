<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationapp2">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:label="NavigationApp"
        android:theme="@style/Theme.NavigationApp2"
        android:icon="@mipmap/ic_app"
        android:roundIcon="@mipmap/ic_app_round">

        <!-- Splash Screen (entry point) -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.NavigationApp2.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main App Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <!-- Navigation History Logs -->
        <activity
            android:name=".LogActivity"
            android:exported="false"/>

        <!-- View UI Tree -->
        <activity
            android:name=".UITreeActivity"
            android:exported="false"/>

        <!-- Device Information -->
        <activity
            android:name=".DeviceInfoActivity"
            android:exported="false"/>

        <!-- Overlay -->
        <service
            android:name=".OverlayService"
            android:exported="false"/>

        <!-- 🔥 Accessibility Service -->
        <service
            android:name=".NavigationAccessibilityService"
            android:label="Navigation Assistant"
            android:icon="@mipmap/ic_app"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>